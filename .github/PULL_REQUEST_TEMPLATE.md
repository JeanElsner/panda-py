## What this changes

<!-- What the change does and why. Link an issue if there is one. -->

## How it was verified

<!--
Was it built against more than one libfranka version, and tested on more than
one Python? Was it tried on a robot, and if so which model and system version?
"Not tested on hardware" is a fine answer, it just needs saying.
-->

## Checklist

- [ ] `pre-commit run --all-files` passes
- [ ] Tests added or updated for anything checkable without a robot
- [ ] Type stubs regenerated with `bin/gen_stubs.sh` if bindings changed
- [ ] `CHANGELOG.md` updated under the unreleased section
